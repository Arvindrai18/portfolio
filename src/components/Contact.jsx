import React from "react";

const ContactButton=({onClick})=>{
    return(
        <button className="contact-button" onClick={onClick}>CONTACT</button>
    );
}

export default ContactButton;