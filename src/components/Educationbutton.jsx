import React from "react";

const Educationbutton = ({ onClick }) => {
  return (
    <button className="education-button" onClick={onClick}>EDUCATION</button>
  );
};

export default Educationbutton;
